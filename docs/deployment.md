# Deployment Guide

## Prerequisites
- Node.js >= 18
- Supabase Project & URL / Service Role Key

## Production Build & Start
```bash
npm run build
npm start
```

## Environment Setup
Ensure all values in `.env.example` are populated in your hosting provider configuration (e.g. Render, Railway, Fly.io, Vercel).
